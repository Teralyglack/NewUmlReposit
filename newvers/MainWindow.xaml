<Window x:Class="UmlEditor.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:UmlEditor"
        mc:Ignorable="d"
        Title="UML Editor" Height="600" Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        


        <!-- Toolbox Panel -->
        <Border Grid.Column="0" Width="80" Background="#f0f0f0" BorderBrush="Gray" BorderThickness="0,0,1,0">
            <StackPanel>
                <TextBlock Text="UML Tools" FontWeight="Bold" Margin="5" HorizontalAlignment="Center"/>

                <!-- Human stick figure -->
                <Button x:Name="HumanButton" ToolTip="Actor" Click="ToolButton_Click" Tag="Human" Margin="5">
                    <Canvas Width="40" Height="60">
                        <Ellipse Canvas.Left="15" Canvas.Top="0" Width="10" Height="10" Stroke="Black" Fill="White"/>
                        <Line X1="20" Y1="10" X2="20" Y2="30" Stroke="Black" StrokeThickness="2"/>
                        <Line X1="5" Y1="20" X2="35" Y2="20" Stroke="Black" StrokeThickness="2"/>
                        <Line X1="20" Y1="30" X2="10" Y2="50" Stroke="Black" StrokeThickness="2"/>
                        <Line X1="20" Y1="30" X2="30" Y2="50" Stroke="Black" StrokeThickness="2"/>
                    </Canvas>
                </Button>

                <!-- Oval -->
                <Button x:Name="OvalButton" ToolTip="Use Case" Click="ToolButton_Click" Tag="Oval" Margin="5">
                    <Ellipse Width="40" Height="30" Stroke="Black" Fill="White"/>
                </Button>

                <!-- Rectangle -->
                <Button x:Name="RectangleButton" ToolTip="Class" Click="ToolButton_Click" Tag="Rectangle" Margin="5">
                    <Rectangle Width="40" Height="30" Stroke="Black" Fill="White"/>
                </Button>

                <!-- Diamond -->
                <Button x:Name="DiamondButton" ToolTip="Decision" Click="ToolButton_Click" Tag="Diamond" Margin="5">
                    <Canvas Width="40" Height="30">
                        <Polygon Points="20,0 40,15 20,30 0,15" Stroke="Black" Fill="White"/>
                    </Canvas>
                </Button>

                <!-- Arrow -->
                <Button x:Name="ArrowButton" ToolTip="Association" Click="ToolButton_Click" Tag="Arrow" Margin="5">
                    <Canvas Width="40" Height="30">
                        <Line X1="5" Y1="15" X2="35" Y2="15" Stroke="Black" StrokeThickness="2"/>
                        <Polygon Points="35,15 25,10 25,20" Stroke="Black" Fill="Black"/>
                    </Canvas>
                </Button>

                <!-- Diamond-tip arrow -->
                <Button x:Name="DiamondArrowButton" ToolTip="Aggregation" Click="ToolButton_Click" Tag="DiamondArrow" Margin="5">
                    <Canvas Width="40" Height="30">
                        <Line X1="5" Y1="15" X2="25" Y2="15" Stroke="Black" StrokeThickness="2"/>
                        <Polygon Points="25,15 35,5 45,15 35,25" Stroke="Black" Fill="White"/>
                    </Canvas>
                </Button>

                <!-- Delete Button -->
                <Button x:Name="DeleteButton" ToolTip="Delete selected (Del)"  Margin="5,20,5,5">
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                        <Path Data="M0,0 L10,10 M10,0 L0,10" Stroke="Red" StrokeThickness="2"/>
                    </StackPanel>
                </Button>
            </StackPanel>
        </Border>

        <!-- Drawing Canvas -->
        <Canvas x:Name="DrawingCanvas" Grid.Column="1" Background="White"
                MouseLeftButtonDown="Canvas_MouseLeftButtonDown"
                MouseMove="Canvas_MouseMove"
                MouseLeftButtonUp="Canvas_MouseLeftButtonUp"
              >
        </Canvas>
        
        
    </Grid>

    <Window.Resources>
        <Style x:Key="DeleteAdornerStyle" TargetType="Button">
            <Setter Property="Content" Value="✕"/>
            <Setter Property="Width" Value="20"/>
            <Setter Property="Height" Value="20"/>
            <Setter Property="Background" Value="#FFFF0000"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="HorizontalAlignment" Value="Right"/>
            <Setter Property="VerticalAlignment" Value="Top"/>
            <Setter Property="Margin" Value="-10,-10,0,0"/>
        </Style>
    </Window.Resources>
</Window>